{
  "name": "@evanwinter/styles",
  "version": "1.1.10",
  "description": "A minimal CSS utility library.",
  "main": "dist/index.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass src/index.scss dist/index.css --style=compressed",
    "release": "npm version patch && npm publish && git push --follow-tags"
  },
  "author": "Evan Winter",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.57.1"
  }
}
